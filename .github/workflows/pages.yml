name: Deploy to GitHub Pages

# Deploys the static site whenever main is updated. The only build step is
# generating llms-full.txt from llms.txt + content/*.md so it stays in sync.
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - name: Generate llms-full.txt
        run: python3 scripts/build_llms_full.py
      - name: Upload site
        uses: actions/upload-pages-artifact@v3
        with:
          path: .   # repo root is the site root (pure static)
      - id: deployment
        uses: actions/deploy-pages@v4
